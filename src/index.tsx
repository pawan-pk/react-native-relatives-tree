import type {
  RelativeItem,
  RelativeItemComponent,
  RelativeItemProps,
  RelativesTreeProps,
} from './types';

import RelativesTree from './RelativesTree';

export type {
  RelativeItem,
  RelativesTreeProps,
  RelativeItemProps,
  RelativeItemComponent,
};

export default RelativesTree;
